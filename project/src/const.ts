export enum AppRoute {
  Login= '/login',
  Property= '/offer/:id',
  Root= '/'
}

export enum CardPage {
  MainPage = 'cities',
  PropertyPage = 'near-places'
}

const MAX_STARS = 5;

export {MAX_STARS};

